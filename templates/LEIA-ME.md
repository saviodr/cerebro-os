# templates/ — modelos reutilizáveis

Guarde aqui modelos que o Claude deve usar como base: estrutura de proposta,
formato de relatório, roteiro padrão. Referencie no CLAUDE.md quando um modelo
virar regra ("toda proposta segue templates/proposta.md").
